<% if object.errors.any? %>

<div>
  <strong>
    <%= pluralize(object.errors.count, 'error') %> was/were found
  </strong>

  <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li class="fields_with_errors"><%= msg %></li>
    <% end %>
  </ul>
</div>

<% end %>
